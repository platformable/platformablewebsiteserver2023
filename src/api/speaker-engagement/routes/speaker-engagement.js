'use strict';

/**
 * speaker-engagement router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::speaker-engagement.speaker-engagement');
